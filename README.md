# GreenWorks Landscaping (local demo)

Run locally:

```bash
npm install
npm start
```

Open http://localhost:3000

Bookings are stored in `bookings.json`. For production add authentication and email confirmations (e.g., nodemailer).